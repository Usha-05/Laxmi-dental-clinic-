"use client"

import DentalSymbolsBackground from "@/components/dental-symbols-background"

export default function About() {
  return (
    <section
      id="about"
      className="py-0 bg-gradient-to-b from-white via-[#ecfdf5] to-[#d1fae5] relative"
    >
      <DentalSymbolsBackground />
      <div className="w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-base sm:text-lg md:text-xl leading-relaxed md:leading-loose">
        {/* Content removed per request */}
      </div>
    </section>
  )
}
